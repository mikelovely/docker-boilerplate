version: "3"

services:
    webserver:
        image: nginx:latest
        ports:
            - "8021:80"
        volumes:
            - ./code:/code
            - ./site.conf:/etc/nginx/conf.d/default.conf
        links:
        - php
    php:
        image: php:7-fpm
        volumes:
            - ./code:/code
